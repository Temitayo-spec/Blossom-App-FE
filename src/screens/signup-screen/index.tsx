import React from 'react';
import { Box, Text } from '@/utils/theme';
import SafeAreaWrapper from '@/components/shared/safe-area-wrapper';

const SignUp = () => {
  return (
    <SafeAreaWrapper>
      <Box>
        <Text variant="textXl">Sign up Screen</Text>
      </Box>
    </SafeAreaWrapper>
  );
};

export default SignUp;
